This is a sample application of Node.
It provides RESTful CRUD functions of 'BOOK' data on mongodb.

METHOD:URI:expression
GET   :/         :index.page.
GET   :/book/(id):get one book data(JSON) whose has id.
PUT   :/book/(id):update one book data whose has id.
DELETE:/book/(id):delete one book data whose has id.
POST  :/book     :create new book data.

